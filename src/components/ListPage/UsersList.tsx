import React, {useEffect, useState} from "react";
import {User} from "../Users/User";
import {NextButton} from "../Buttons/NextButton";
import {UserType} from "../../store/usersReducer";



export const UsersList = (mailUser: any) => {

    return (
        <div>

            <NextButton/>
        </div>
    )
}